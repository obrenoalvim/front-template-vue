# Base URL of the Laravel API this frontend consumes.
# `php artisan serve` defaults to :8000; back-template-laravel's docker-compose maps to :8080.
VITE_API_URL=http://localhost:8080

# Public URL this frontend is served from. Used for canonical links, OG tags, and the sitemap.
VITE_SITE_URL=http://localhost:5173
